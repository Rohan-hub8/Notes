<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Notes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/a5546973d1.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body class="bg-black">
    <div class="container-fluid" style="height: 100vh;">
        <div class="container">
            <form action="/create" method="post" class="py-4">
                <input class="form-control mb-3 bg-transparent text-white border-secondary custom-placeholder"
                    type="text" placeholder="Title goes here.." name="title" required>
                <textarea class="form-control mb-3 bg-transparent text-white border-secondary custom-placeholder"
                    rows="8" style="resize: none;" placeholder="Write your task details here..."
                    name="detail"></textarea>
                <button class="btn btn-secondary">Create Note</button>
            </form>

            <div class="row p-3 flex-wrap gap-2">
                <% if(files.length> 0) { %>
                    <% files.forEach((file)=> { %>
                        <div class="col-12 col-lg-3 bg-dark rounded py-2 note">
                            <h2 class="text-white-50">
                                <%= file.replace(/\.txt$/, '' ) %>
                            </h2>
                            <div class="d-flex justify-content-between align-items-center">
                                <a href="/note/<%= file %>" class="text-decoration-none">read more</a>
                                <form action="/delete/<%= file %>" method="post">
                                    <button class="btn text-danger"><i class="fa-solid fa-trash"></i></button>
                                </form>
                            </div>
                        </div>
                        <% }); %>
                            <% } else { %>
                                <h4 class="text-body-tertiary">no tasks yet.</h4>
                                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>